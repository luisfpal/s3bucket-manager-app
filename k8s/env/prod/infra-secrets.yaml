# Production secrets for Authentik infrastructure (authentik-bucket-explorer namespace).
# Managed by infrastructure administrators — fill in all REPLACE_WITH_PROD_* values.
apiVersion: v1
kind: Secret
metadata:
  name: authentik-secret
  namespace: authentik-bucket-explorer
type: Opaque
stringData:
  secret-key: "REPLACE_WITH_PROD_AUTHENTIK_SECRET_KEY"
  bootstrap-password: "REPLACE_WITH_PROD_AUTHENTIK_BOOTSTRAP_PASSWORD"
  postgres-password: "REPLACE_WITH_PROD_AUTHENTIK_POSTGRES_PASSWORD"
  bootstrap-token: "REPLACE_WITH_PROD_AUTHENTIK_BOOTSTRAP_TOKEN"
